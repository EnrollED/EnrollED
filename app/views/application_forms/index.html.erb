<% if !policy(:admin).index? %>
    <h1>Seznam prijav</h1>

    <table>
      <thead>
      <tr>
        <th></th>
        <th>Stanje</th>
        <th>Datum oddaje</th>
        <th>Univerza</th>
        <th>Visokošolski zavod</th>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @applications.each_with_index do |application, index| %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= application.status %></td>
            <td><%= application.submission_date %></td>

          </tr>
      <% end %>
      </tbody>
    </table>

    <%= link_to 'Nova prijava', new_application_form_path %>
<% else %>
    Admin
<% end %>