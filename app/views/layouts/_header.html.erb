<nav class="navbar navbar-inverse navbar-static-top" style="margin-bottom: 0">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav" aria-expanded="false">
        <span class="sr-only"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <a class="navbar-brand" href="<%= root_path %>"><%= t 'defaults.name' %></a>
    </div>

    <div class="collapse navbar-collapse" id="nav">

      <ul class="nav navbar-nav navbar-right">
        <% if policy(:study_program).index? %>
            <li>
              <a href="<%= study_programs_path %>"><%= "Študijski programi" %></a>
            </li>
        <% end %>

        <% if policy(:post).index? %>
            <li>
              <a href="<%= codes_path %>" data-toggle="tooltip" rel="tooltip" data-placement="bottom"
                 title="<%= t '.admin_area' %> ">
                <i class="fa fa-wrench fa-lg" aria-hidden="true"></i>
              </a>
            </li>
        <% end %>

        <li class="dropdown dropdown-user">
          <a class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            <%= image_tag gravatar_url(current_user.email) %>
          </a>
          <ul class="dropdown-menu">
            <li class="current-user">
              <a data-toggle="tooltip" rel="tooltip" data-placement="left"
                 title="<%= t '.zadnja_prijava' %>: <%= l current_user.last_sign_in_at, format: :short %>">
                <%= current_user.firstname + ' ' + current_user.lastname %>
              </a>
            </li>
            <li class="divider"></li>
            <li>
              <a href="<%= edit_user_path(current_user) %>"><%= t '.my_profile' %></a>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>