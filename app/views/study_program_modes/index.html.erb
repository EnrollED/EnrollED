<h1>Študijski programi - razpisana mesta</h1>

<div class="row">

  <div class="col-sm-4">
    <%= form_tag(study_programs_path, method: :get) do |f| %>

        <div class="input-group">

          <%= search_field_tag :search, params[:search], autofocus: true,
                               placeholder: t('defaults.search') + '...', class: 'form-control' %>

          <span class="input-group-btn">
            <button class="btn btn-primary" type="submit"><i class="fa fa-search"></i></button>
          </span>

        </div>

    <% end %>
  </div>

  <div class="col-sm-8 text-right">
    <%= link_to "Dodaj razpisana mesta", new_study_program_mode_path, class: 'btn btn-success' %>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead>
    <tr>
      <th>Fakulteta</th>
      <th>Način študija</th>
      <th>Ime programa</th>
      <th>Vrsta</th>
      <th>EU</th>
      <th>Tujci</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <% @study_program_modes.each do |study_program_mode| %>
        <tr>
          <td><%= study_program_mode.study_program.higher_education_institution.acronym %></td>
          <td><%= study_program_mode.study_program.type_of_study.name %></td>
          <td><%= study_program_mode.study_program.name %></td>
          <td><%= study_program_mode.mode_of_study.name %></td>
          <td><%= study_program_mode.number_of_places %></td>
          <td><%= study_program_mode.number_of_places_foreign %></td>
          <td>
            <%= link_to study_program_mode_path(study_program_mode), class: 'label label-success' do %>
                <i class="fa fa-eye" title="<%= "Prikaži" %>" aria-hidden="true"></i>
            <% end %>
          </td>
          <td>
            <%= link_to edit_study_program_mode_path(study_program_mode), class: 'label label-info' do %>
                <i class="fa fa-pencil" title="<%= t 'defaults.edit' %>" aria-hidden="true"></i>
            <% end %>
          </td>
          <td>
            <%= link_to study_program_mode_path(study_program_mode), method: :delete, data: { confirm: 'Ali ste prepričani?' }, class: 'label label-danger' do %>
                <i class="fa fa-trash" title="<%= "Odstrsani" %>" aria-hidden="true"></i>
            <% end %>
          </td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>

<div>
  <%= paginate @study_program_modes %>
</div>
