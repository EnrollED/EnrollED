<h1><%= t '.edit' %></h1>

<%= render 'admin/users/form', user: @user %>

<%= link_to  t('defaults.cancel'), :back, class: 'btn btn-warning' %>

<%= button_to "Cancel my account", registration_path(:user),
              data: { confirm: t('defaults.are_you_sure') }, class: 'btn btn-danger', method: :delete %>
