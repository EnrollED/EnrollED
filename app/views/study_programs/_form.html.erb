<%= form_for(@study_program) do |f| %>
  <% if @study_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_program.errors.count, "error") %> prohibited this study_program from being saved:</h2>

      <ul>
      <% @study_program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
